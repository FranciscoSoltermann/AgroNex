const axios = require('axios');
const fs = require('fs');

async function test() {
    try {
        const email = "franciscosoltermann@agronex.com";
        const password = "password123"; 

        // Let's just try to do the PUT request using the SUPABASE_ANON_KEY to login if we can, 
        // but wait, I can just use supabase client directly.
    } catch (e) {
        console.error(e);
    }
}
test();
